// The Library stdio.h meant Standard IO (Input/Output) Library.
#include <stdio.h>

// On future programs you will notice that you can use void (I think) instead of Int. (Correction Ahhhh! No! You will need to use int and return int because this can lead to portability issues with the OS).
// And remember: "main is special!"
int main()
{
    printf("Hello World \n");
    return 0;
}
